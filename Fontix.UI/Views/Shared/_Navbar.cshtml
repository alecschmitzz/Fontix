@inject Fontix.UI.Utils.ISessionAccess SessionAccess
<div class="fontix-nav collapsed" style="z-index: 1000;">
    <nav class="collapsed">
        <div class="burger collapsed d-flex align-items-center">

            <p class="pe-3 text-fontix-blue fw-bold m-0 menu-open">menu</p>
            <p class="pe-3 text-fontix-blue fw-bold m-0 menu-close">sluiten</p>

            <button type="button" class="btn btn-primary p-0 pt-1">
                <i class="fas fa-bars"></i>
                <i class="fas fa-close"></i>
            </button>

        </div>
        <div class="menu">
            <ul>
                <li data-aos="fade-up" data-aos-delay="300">
                    <a href="@Url.Action("Index", "Events")">
                        <span>events</span>
                    </a>
                </li>
                @if (SessionAccess.GetUserId() > 0)
                {
                    <li class="text-fontix-purple mt-5" style="font-size: 1rem;">
                        admin
                    </li>
                    <li data-aos="fade-up" data-aos-delay="300">
                        <a href="@Url.Action("Manage", "EventsAdmin")">
                            <span>events</span>
                        </a>
                    </li>
                    <li data-aos="fade-up" data-aos-delay="450">
                        <a href="@Url.Action("Manage", "OrganisationsAdmin")">
                            <span>organisations</span>
                        </a>
                    </li>
                }
                else
                {
                    <li data-aos="fade-up" data-aos-delay="300">
                        <a href="@Url.Action("Login", "Users")">
                            <span>Login</span>
                        </a>
                    </li>
                }
            </ul>
        </div>
        <div class="menu-bg"></div>
    </nav>
</div>